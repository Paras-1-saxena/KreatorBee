<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
            <record model="ir.ui.view" id="accounting_reports.list">
              <field name="name">accounting_reports list</field>
              <field name="model">accounting_reports.accounting_reports</field>
              <field name="arch" type="xml">
                <list>
                  <field name="name"/>
                  <field name="value"/>
                  <field name="value2"/>
                </list>
              </field>
            </record>
        -->

        <!-- actions opening views on models -->
        <!--
            <record model="ir.actions.act_window" id="accounting_reports.action_window">
              <field name="name">accounting_reports window</field>
              <field name="res_model">accounting_reports.accounting_reports</field>
              <field name="view_mode">list,form</field>
            </record>
        -->

        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="accounting_reports.action_server">
              <field name="name">accounting_reports server</field>
              <field name="model_id" ref="model_accounting_reports_accounting_reports"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "list,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->
        <!--
            <menuitem name="accounting_reports" id="accounting_reports.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="accounting_reports.menu_1" parent="accounting_reports.menu_root"/>
            <menuitem name="Menu 2" id="accounting_reports.menu_2" parent="accounting_reports.menu_root"/>
        -->
        <!-- actions -->
        <!--
            <menuitem name="List" id="accounting_reports.menu_1_list" parent="accounting_reports.menu_1"
                      action="accounting_reports.action_window"/>
            <menuitem name="Server to list" id="accounting_reports" parent="accounting_reports.menu_2"
                      action="accounting_reports.action_server"/>
        -->

        <record id="view_partner_form_custom" model="ir.ui.view">
            <field name="name">res.partner.form.custom</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base_vat.view_partner_base_vat_form"/>
            <field name="arch" type="xml">
                <!-- Change the label of the vat field -->
                <xpath expr="//label[@for='vat']" position="attributes">
                    <attribute name="string">TIN</attribute>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
